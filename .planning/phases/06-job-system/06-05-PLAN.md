---
phase: 06-job-system
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/types/job.ts
  - web/src/api/jobs.ts
  - web/src/components/dag/DAGCanvas.tsx
  - web/src/components/jobs/StepEditor.tsx
  - web/src/views/JobEditor.tsx
  - web/src/views/JobsPage.tsx
  - web/src/views/RunDetail.tsx
  - web/src/stores/runs.ts
autonomous: true
gap_closure: true
requirements: [JOBS-01, JOBS-02, JOBS-03, JOBS-04]

must_haves:
  truths:
    - "Frontend type field names match backend JSON tags (job_id, step_id, run_id, run_step_id, depends_on)"
    - "DAG canvas renders edges using correct dependency field names"
    - "Step selection, navigation, and retry all resolve entity IDs correctly"
    - "addDependency sends {depends_on: ...} matching backend expectation"
  artifacts:
    - path: "web/src/types/job.ts"
      provides: "TypeScript interfaces aligned with backend JSON"
      contains: "job_id"
    - path: "web/src/api/jobs.ts"
      provides: "Correct addDependency payload"
      contains: "depends_on"
    - path: "web/src/components/dag/DAGCanvas.tsx"
      provides: "Correct edge source/target using depends_on field"
      contains: "dep.depends_on"
  key_links:
    - from: "web/src/types/job.ts"
      to: "web/src/views/JobEditor.tsx"
      via: "step_id field for selection and navigation"
      pattern: "step_id"
    - from: "web/src/types/job.ts"
      to: "web/src/components/dag/DAGCanvas.tsx"
      via: "step_id for node IDs, depends_on for edge source"
      pattern: "step\\.step_id|dep\\.depends_on"
    - from: "web/src/api/jobs.ts"
      to: "backend handler"
      via: "addDependency POST body"
      pattern: "depends_on.*dependsOnStepId"
---

<objective>
Align frontend TypeScript types with backend JSON field names to fix all entity ID resolution.

Purpose: The frontend declares `id` on Job/Step/Run/RunStep but the backend serializes `job_id`/`step_id`/`run_id`/`run_step_id`. Every entity lookup, navigation, DAG edge, and retry check resolves to `undefined` against real server data. The addDependency call sends `depends_on_step_id` but the backend expects `depends_on`.
Output: All frontend types use backend field names; all views and components reference the correct fields.
</objective>

<execution_context>
@/home/joseibanez/.claude/get-shit-done/workflows/execute-plan.md
@/home/joseibanez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-job-system/06-VERIFICATION.md
@.planning/phases/06-job-system/06-03-SUMMARY.md

<interfaces>
<!-- Backend JSON field names (the source of truth) -->

From internal/server/store/jobs.go:
```go
type Job struct {
    JobID       string    `json:"job_id"`
    Name        string    `json:"name"`
    Description string    `json:"description"`
    UserID      string    `json:"user_id"`
    CreatedAt   time.Time `json:"created_at"`
    UpdatedAt   time.Time `json:"updated_at"`
}

type Step struct {
    StepID    string `json:"step_id"`
    JobID     string `json:"job_id"`
    Name      string `json:"name"`
    // ...other fields...
}

type StepDependency struct {
    StepID    string `json:"step_id"`
    DependsOn string `json:"depends_on"`
}

type Run struct {
    RunID       string     `json:"run_id"`
    JobID       string     `json:"job_id"`
    Status      string     `json:"status"`
    // ...
}

type RunStep struct {
    RunStepID string     `json:"run_step_id"`
    RunID     string     `json:"run_id"`
    StepID    string     `json:"step_id"`
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript types and API client to match backend JSON</name>
  <files>web/src/types/job.ts, web/src/api/jobs.ts</files>
  <action>
1. In `web/src/types/job.ts`, rename fields to match backend JSON tags:

   - `Job.id` -> `Job.job_id`
   - `Step.id` -> `Step.step_id`
   - `StepDependency.id` -> remove (backend has no `id` field; the PK is composite `step_id + depends_on`)
   - `StepDependency.depends_on_step_id` -> `StepDependency.depends_on`
   - `Run.id` -> `Run.run_id`
   - `Run.finished_at` -> keep as-is if backend uses `completed_at` then rename to `completed_at`
   - `RunStep.id` -> `RunStep.run_step_id`
   - `RunStep.finished_at` -> check backend; if `completed_at` then rename

   Check the backend Run struct: it uses `completed_at` (not `finished_at`). Fix `Run.finished_at` to `Run.completed_at`. Same for RunStep if applicable.

   The backend RunStep has `exit_code`, `started_at`, `completed_at` fields. RunStep in frontend has `finished_at` -- rename to `completed_at`. RunStep does not have `error` in backend but frontend has it -- keep it as an optional field for future use.

2. In `web/src/api/jobs.ts`:
   - Line 50: Change `{ depends_on_step_id: dependsOnStepId }` to `{ depends_on: dependsOnStepId }`
   - The `removeDependency` path uses `depId` -- this should be the `depends_on` step ID. The backend route is `DELETE /api/v1/jobs/{jobID}/steps/{stepID}/deps/{depID}` where depID is the depends_on step ID. This is correct as-is.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane/web && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <done>All TypeScript interfaces use backend-aligned field names; addDependency sends {depends_on: ...}</done>
</task>

<task type="auto">
  <name>Task 2: Update all frontend consumers to use new field names</name>
  <files>web/src/components/dag/DAGCanvas.tsx, web/src/components/jobs/StepEditor.tsx, web/src/views/JobEditor.tsx, web/src/views/JobsPage.tsx, web/src/views/RunDetail.tsx, web/src/stores/runs.ts</files>
  <action>
Update every file that references the old field names. The complete list of changes:

**web/src/components/dag/DAGCanvas.tsx:**
- Line 87: `step.id` -> `step.step_id` (node ID and runStepMap lookup)
- Line 89: `step.id` -> `step.step_id` (ReactFlow node id)
- Line 96: `step.id === selectedStepId` -> `step.step_id === selectedStepId`
- Line 102: `dep.depends_on_step_id` -> `dep.depends_on` (edge source + runStepMap lookup)
- Line 104: `dep.id` -> generate a synthetic edge ID like `${dep.depends_on}->${dep.step_id}` (backend StepDependency has no separate id field)
- Line 105: `dep.depends_on_step_id` -> `dep.depends_on` (edge source)

**web/src/components/jobs/StepEditor.tsx:**
- Line 32: `step.id` -> `step.step_id` (onSave callback)
- Lines 46, 58, 70, 89, 102, 114: `step.id` -> `step.step_id` (React key props)
- Line 128: `step.id` -> `step.step_id` (onDelete callback)

**web/src/views/JobEditor.tsx:**
- Line 49: `jobDetail.job.id` -> `jobDetail.job.job_id`
- Line 61: `s.id === selectedStepId` -> `s.step_id === selectedStepId`
- Line 71: `job.id` -> `job.job_id` (after create)
- Line 72: `job.id` -> `job.job_id` (navigate)
- Line 73: `job.id` -> `job.job_id` (return)
- Line 91: `step.id` -> `step.step_id` (selectStep after add)
- Line 138: `run.id` -> `run.run_id` (navigate to run)

**web/src/views/JobsPage.tsx:**
- Line 26: `run.id` -> `run.run_id`
- Line 80: `job.id` -> `job.job_id` (React key)
- Line 81: `job.id` -> `job.job_id` (navigate)
- Line 90: `job.id` -> `job.job_id` (display truncateId)
- Line 104: `job.id` -> `job.job_id` (handleRun)

**web/src/views/RunDetail.tsx:**
- Line 57: `s.id === selectedStepId` -> `s.step_id === selectedStepId` (step name lookup)
- Line 88-91: `run.finished_at` -> `run.completed_at` (elapsed time calc) -- if this field was renamed in Task 1

**web/src/stores/runs.ts:**
- Line 36: Remove `id: ''` from the fallback RunStep creation. Replace with `run_step_id: ''` to match the renamed type. The full fallback object should be:
  ```typescript
  {
    run_step_id: '',
    run_id: '',
    step_id: stepId,
    status: status as RunStep['status'],
    ...(sessionId ? { session_id: sessionId } : {}),
  }
  ```

After all changes, run `npx tsc --noEmit` to verify no type errors remain. If there are additional references missed, fix them until TypeScript compiles clean.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane/web && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All frontend files reference correct field names (step_id, job_id, run_id, run_step_id, depends_on); TypeScript compiles with zero errors; existing tests pass</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` exits 0 (no type errors)
- `cd web && npx vitest run` passes all tests
- `grep -rn '\.id\b' web/src/types/job.ts` shows no bare `id` fields (only `job_id`, `step_id`, etc.)
- `grep -rn 'depends_on_step_id' web/src/` returns no matches
- `grep -rn 'dep\.id\b' web/src/components/dag/DAGCanvas.tsx` returns no matches
</verification>

<success_criteria>
- Frontend TypeScript types exactly match backend Go JSON tags for all entity ID fields
- addDependency API call sends `{ depends_on: ... }` not `{ depends_on_step_id: ... }`
- DAG canvas builds edges using `dep.depends_on` as source and `dep.step_id` as target
- Job navigation after create uses `job.job_id`
- Step selection in editor uses `step.step_id`
- Run navigation uses `run.run_id`
- Retry button visibility resolves correctly via `run_step_id` field
- TypeScript compiles clean, all Vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-job-system/06-05-SUMMARY.md`
</output>
