---
phase: 05-frontend
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - web/package.json
  - web/vite.config.ts
  - web/src/__tests__/setup.ts
  - web/src/__tests__/views/CommandCenter.test.tsx
  - web/src/__tests__/views/SessionsPage.test.tsx
  - web/src/__tests__/views/MachinesPage.test.tsx
  - web/src/__tests__/components/sessions/NewSessionModal.test.tsx
autonomous: true
requirements:
  - SESS-04

must_haves:
  truths:
    - "Vitest runs and exits cleanly with 0 failures"
    - "Test setup file configures jsdom environment and testing-library matchers"
    - "Stub test files exist for all views and key components"
  artifacts:
    - path: "web/src/__tests__/setup.ts"
      provides: "Test setup with jsdom, @testing-library/jest-dom matchers"
    - path: "web/src/__tests__/views/CommandCenter.test.tsx"
      provides: "Stub test for CommandCenter dashboard"
    - path: "web/src/__tests__/views/SessionsPage.test.tsx"
      provides: "Stub test for SessionsPage"
    - path: "web/src/__tests__/views/MachinesPage.test.tsx"
      provides: "Stub test for MachinesPage"
    - path: "web/src/__tests__/components/sessions/NewSessionModal.test.tsx"
      provides: "Stub test for NewSessionModal"
  key_links:
    - from: "web/vite.config.ts"
      to: "web/src/__tests__/setup.ts"
      via: "Vitest test.setupFiles configuration"
      pattern: "setupFiles.*setup"
---

<objective>
Install test dependencies and create Vitest configuration with stub test files for all views and key components. This is Wave 0 infrastructure that subsequent plans' verify blocks depend on.

Purpose: VALIDATION.md requires Vitest-based automated verification. Without this Wave 0, no plan can run unit tests.
Output: `npx vitest run` executes and passes with stub tests for CommandCenter, SessionsPage, MachinesPage, and NewSessionModal.
</objective>

<execution_context>
@/home/joseibanez/.claude/get-shit-done/workflows/execute-plan.md
@/home/joseibanez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-frontend/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test deps and configure Vitest</name>
  <files>web/package.json, web/vite.config.ts, web/src/__tests__/setup.ts</files>
  <action>
1. Install test dependencies as devDependencies:
   ```bash
   cd web && npm install -D @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
   ```

2. Update `web/vite.config.ts` to add Vitest configuration in the `test` block:
   ```typescript
   /// <reference types="vitest/config" />
   // ... existing imports and config ...
   export default defineConfig({
     // ... existing plugins, build, server config ...
     test: {
       globals: true,
       environment: 'jsdom',
       setupFiles: ['./src/__tests__/setup.ts'],
       include: ['src/**/*.test.{ts,tsx}'],
     },
   });
   ```
   Preserve all existing config (plugins, build.outDir, server.proxy, etc.). Only add the `test` block.

3. Create `web/src/__tests__/setup.ts`:
   ```typescript
   import '@testing-library/jest-dom';
   ```
   This imports the custom matchers (toBeInTheDocument, toHaveTextContent, etc.) globally for all tests.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane/web && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Vitest configured in vite.config.ts with jsdom environment. Test setup file imports jest-dom matchers. `npx vitest run` executes without configuration errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create stub test files for views and components</name>
  <files>web/src/__tests__/views/CommandCenter.test.tsx, web/src/__tests__/views/SessionsPage.test.tsx, web/src/__tests__/views/MachinesPage.test.tsx, web/src/__tests__/components/sessions/NewSessionModal.test.tsx</files>
  <action>
Create stub test files that will be fleshed out as actual components are built. Each stub imports `describe` and `it` from vitest and contains a single placeholder test that passes. This ensures the test runner finds files and the directory structure is ready.

1. Create `web/src/__tests__/views/CommandCenter.test.tsx`:
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('CommandCenter', () => {
     it('stub: will test dashboard rendering', () => {
       expect(true).toBe(true);
     });
   });
   ```

2. Create `web/src/__tests__/views/SessionsPage.test.tsx`:
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('SessionsPage', () => {
     it('stub: will test session listing', () => {
       expect(true).toBe(true);
     });
   });
   ```

3. Create `web/src/__tests__/views/MachinesPage.test.tsx`:
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('MachinesPage', () => {
     it('stub: will test machine status display', () => {
       expect(true).toBe(true);
     });
   });
   ```

4. Create `web/src/__tests__/components/sessions/NewSessionModal.test.tsx`:
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('NewSessionModal', () => {
     it('stub: will test session creation modal', () => {
       expect(true).toBe(true);
     });
   });
   ```
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane/web && npx vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>All 4 stub test files exist and pass. `npx vitest run` reports 4 test suites, 4 tests passed, 0 failures.</done>
</task>

</tasks>

<verification>
- `cd web && npx vitest run --reporter=verbose` -- all stub tests pass
- Test setup file imports jest-dom matchers
- Vitest config uses jsdom environment
</verification>

<success_criteria>
Vitest runs successfully with jsdom environment. All 4 stub test files pass. Test infrastructure is ready for subsequent plans to add real tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-00-SUMMARY.md`
</output>
