---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - buf.yaml
  - buf.gen.yaml
  - proto/claudeplane/v1/agent.proto
  - internal/shared/proto/claudeplane/v1/agent.pb.go
  - internal/shared/proto/claudeplane/v1/agent_grpc.pb.go
  - cmd/server/main.go
  - cmd/agent/main.go
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "go build ./cmd/server compiles without errors"
    - "go build ./cmd/agent compiles without errors"
    - "Both binaries produce executables with no external runtime dependencies"
    - "Generated protobuf Go code is importable by both binaries"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition"
      contains: "module github.com/kodrunhq/claude-plane"
    - path: "proto/claudeplane/v1/agent.proto"
      provides: "gRPC service and message definitions"
      contains: "service AgentService"
    - path: "cmd/server/main.go"
      provides: "Server binary entrypoint"
      contains: "func main()"
    - path: "cmd/agent/main.go"
      provides: "Agent binary entrypoint"
      contains: "func main()"
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/shared/proto/claudeplane/v1"
      via: "import of generated proto package"
      pattern: "claudeplane/v1"
    - from: "cmd/agent/main.go"
      to: "internal/shared/proto/claudeplane/v1"
      via: "import of generated proto package"
      pattern: "claudeplane/v1"
---

<objective>
Scaffold the Go module, define the complete protobuf contract, generate Go code from it, and create minimal compilable entrypoints for both server and agent binaries.

Purpose: Establishes the shared foundation (module, proto types, binary shells) that every subsequent plan and phase depends on.
Output: Compilable Go module with two binaries and generated protobuf code.
</objective>

<execution_context>
@/home/joseibanez/.claude/get-shit-done/workflows/execute-plan.md
@/home/joseibanez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@docs/internal/product/backend_v1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module, install dependencies, configure buf, define protobuf contract</name>
  <files>go.mod, go.sum, buf.yaml, buf.gen.yaml, proto/claudeplane/v1/agent.proto, internal/shared/proto/claudeplane/v1/agent.pb.go, internal/shared/proto/claudeplane/v1/agent_grpc.pb.go</files>
  <action>
1. Initialize Go module: `go mod init github.com/kodrunhq/claude-plane`

2. Install all Phase 1 dependencies:
   - `modernc.org/sqlite`, `google.golang.org/grpc`, `google.golang.org/protobuf`
   - `github.com/BurntSushi/toml`, `github.com/spf13/cobra`
   - `golang.org/x/crypto`, `github.com/golang-jwt/jwt/v5`

3. Create `buf.yaml` (v2 format):
   - Module path: `proto`
   - Lint: DEFAULT rules
   - Breaking: FILE rules

4. Create `buf.gen.yaml` (v2 format):
   - Managed mode enabled with `go_package_prefix: github.com/kodrunhq/claude-plane/internal/shared/proto`
   - Plugin `buf.build/protocolbuffers/go` output to `internal/shared/proto` with `paths=source_relative`
   - Plugin `buf.build/grpc/go` output to `internal/shared/proto` with `paths=source_relative`

5. Create `proto/claudeplane/v1/agent.proto` with the COMPLETE protobuf contract from backend_v1.md:
   - `service AgentService` with `Register` (unary) and `CommandStream` (bidirectional streaming) RPCs
   - All message types: `RegisterRequest/Response`, `AgentEvent` (oneof: session_output, session_status, health_report, scrollback_chunk, step_output), `ServerCommand` (oneof: create_session, terminate_session, input_data, resize_terminal, attach_session, detach_session, request_scrollback, execute_step)
   - All sub-messages for each oneof variant as defined in backend_v1.md Section 3
   - Use `google.protobuf.Timestamp` for time fields
   - Set `option go_package` to match buf managed mode output path

6. Install buf CLI if not present: `go install github.com/bufbuild/buf/cmd/buf@latest`
   Also install protoc-gen-go and protoc-gen-go-grpc:
   `go install google.golang.org/protobuf/cmd/protoc-gen-go@latest`
   `go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest`

7. Run `buf generate` to produce Go code in `internal/shared/proto/claudeplane/v1/`

8. Run `go mod tidy` to clean up dependencies.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane && buf lint proto && ls internal/shared/proto/claudeplane/v1/agent.pb.go internal/shared/proto/claudeplane/v1/agent_grpc.pb.go</automated>
  </verify>
  <done>Proto files lint clean, generated Go code exists at expected paths, go.mod has all Phase 1 dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create server and agent binary entrypoints with cobra CLI skeleton</name>
  <files>cmd/server/main.go, cmd/agent/main.go</files>
  <action>
1. Create `cmd/server/main.go`:
   - Import cobra, create root command `claude-plane-server` with short description
   - Add placeholder subcommands: `newServeCmd()` (prints "not implemented"), `newCACmd()` parent with `newCAInitCmd()`, `newCAIssueServerCmd()`, `newCAIssueAgentCmd()` children (all print "not implemented"), `newSeedAdminCmd()` (prints "not implemented")
   - Add `--version` flag that prints "claude-plane-server v0.1.0-dev"
   - Import the generated proto package to prove it compiles: `_ "github.com/kodrunhq/claude-plane/internal/shared/proto/claudeplane/v1"`
   - Use `log/slog` for any logging
   - Exit with os.Exit(1) on command error

2. Create `cmd/agent/main.go`:
   - Import cobra, create root command `claude-plane-agent` with short description
   - Add placeholder subcommand: `newRunCmd()` (prints "not implemented")
   - Add `--version` flag that prints "claude-plane-agent v0.1.0-dev"
   - Import generated proto package same way
   - Exit with os.Exit(1) on command error

Both binaries must compile with `go build ./cmd/server` and `go build ./cmd/agent`.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane && go build -o /dev/null ./cmd/server && go build -o /dev/null ./cmd/agent && echo "BOTH BINARIES COMPILE"</automated>
  </verify>
  <done>Both `go build ./cmd/server` and `go build ./cmd/agent` succeed. Each binary has a cobra CLI with version flag and placeholder subcommands. Generated proto package is imported and compiles.</done>
</task>

</tasks>

<verification>
- `buf lint proto` passes with no errors
- `go build ./cmd/server` produces binary
- `go build ./cmd/agent` produces binary
- `go vet ./...` passes
- Generated proto files exist at `internal/shared/proto/claudeplane/v1/`
</verification>

<success_criteria>
Both binaries compile from a single Go module. The protobuf contract is complete (all messages from backend_v1.md), generated, and importable. The module has all Phase 1 dependencies installed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
