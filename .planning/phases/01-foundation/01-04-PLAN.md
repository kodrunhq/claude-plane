---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/server/main.go
  - cmd/agent/main.go
autonomous: true
gap_closure: true
requirements:
  - INFR-03
  - INFR-04
  - AGNT-01
  - AUTH-04

must_haves:
  truths:
    - "claude-plane-server ca init generates CA cert and key PEM files in the specified output directory"
    - "claude-plane-server ca issue-server generates a server certificate signed by the CA"
    - "claude-plane-server ca issue-agent generates an agent certificate with machine-id in CN"
    - "claude-plane-server serve loads config from TOML, initializes SQLite with WAL mode, and creates all schema tables"
    - "claude-plane-server seed-admin creates an admin user with Argon2id-hashed password in SQLite"
    - "claude-plane-agent run loads config from TOML and fails with clear error on missing required fields"
  artifacts:
    - path: "cmd/server/main.go"
      provides: "Server binary with all CLI commands wired to library implementations"
      contains: "tlsutil.GenerateCA"
    - path: "cmd/agent/main.go"
      provides: "Agent binary with config loading wired into run command"
      contains: "config.LoadAgentConfig"
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/shared/tlsutil"
      via: "ca commands call GenerateCA, IssueServerCert, IssueAgentCert"
      pattern: "tlsutil\\."
    - from: "cmd/server/main.go"
      to: "internal/server/store"
      via: "serve and seed-admin commands call NewStore, SeedAdmin"
      pattern: "store\\."
    - from: "cmd/server/main.go"
      to: "internal/server/config"
      via: "serve command calls LoadServerConfig"
      pattern: "config\\.LoadServerConfig"
    - from: "cmd/agent/main.go"
      to: "internal/agent/config"
      via: "run command calls LoadAgentConfig"
      pattern: "config\\.LoadAgentConfig"
---

<objective>
Wire all stub cobra CLI handlers in both binaries to their existing, tested library implementations. This closes every verification gap from Phase 1 (SC2-SC5).

Purpose: The libraries (tlsutil, config, store) are fully implemented and tested but unreachable from the CLI. This plan connects them so the binaries actually work as documented.
Output: Both binaries with functional CLI commands — no more "not implemented" stubs on the server side.
</objective>

<execution_context>
@/home/joseibanez/.claude/get-shit-done/workflows/execute-plan.md
@/home/joseibanez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VERIFICATION.md

<interfaces>
<!-- Existing library functions that must be called from cobra handlers -->

From internal/shared/tlsutil/ca.go:
```go
func GenerateCA(outDir string) error
func IssueServerCert(caDir, outDir string, hostnames []string) error
func IssueAgentCert(caDir, outDir, machineID string) error
```

From internal/server/store/db.go:
```go
type Store struct { /* writer, reader *sql.DB */ }
func NewStore(dbPath string) (*Store, error)
func (s *Store) Close() error
```

From internal/server/store/users.go:
```go
func (s *Store) SeedAdmin(email, password, displayName string) error
```

From internal/server/config/config.go:
```go
func LoadServerConfig(path string) (*ServerConfig, error)
// ServerConfig has fields: HTTP.Listen, GRPC.Listen, TLS.CACert, Database.Path, etc.
```

From internal/agent/config/config.go:
```go
func LoadAgentConfig(path string) (*AgentConfig, error)
// AgentConfig has fields: Server.Address, TLS.CACert, Agent.MachineID, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire server CLI commands to library implementations</name>
  <files>cmd/server/main.go</files>
  <action>
Replace all "not implemented" stubs in cmd/server/main.go with real implementations that call the existing libraries. Add necessary imports for tlsutil, store, and config packages.

1. **newCAInitCmd**: Add `--out-dir` flag (string, required, default "./ca"). In RunE, call `tlsutil.GenerateCA(outDir)`. On success, print "CA initialized in {outDir}". On error, return wrapped error.

2. **newCAIssueServerCmd**: Add flags: `--ca-dir` (string, required, default "./ca"), `--out-dir` (string, required, default "./server-cert"), `--hostnames` (string slice, default ["localhost", "127.0.0.1"]). In RunE, call `tlsutil.IssueServerCert(caDir, outDir, hostnames)`. On success, print "Server certificate issued in {outDir}". On error, return wrapped error.

3. **newCAIssueAgentCmd**: Add flags: `--ca-dir` (string, required, default "./ca"), `--out-dir` (string, required, default "./agent-cert"), `--machine-id` (string, required, no default). In RunE, validate machine-id is not empty (return error if so), then call `tlsutil.IssueAgentCert(caDir, outDir, machineID)`. On success, print "Agent certificate issued in {outDir} for machine {machineID}". On error, return wrapped error.

4. **newSeedAdminCmd**: Add flags: `--db` (string, required, default "claude-plane.db"), `--email` (string, required, no default), `--password` (string, required, no default), `--name` (string, default "Admin"). In RunE, validate email and password are not empty, call `store.NewStore(dbPath)` to open the database (this also runs migrations), defer `s.Close()`, call `s.SeedAdmin(email, password, name)`. On success, print "Admin account created for {email}". On error, return wrapped error.

5. **newServeCmd**: Add `--config` flag (string, required, default "server.toml"). In RunE, call `config.LoadServerConfig(configPath)`. Then call `store.NewStore(cfg.Database.Path)` to initialize the database with WAL mode and run migrations, defer `s.Close()`. Print "Server initialized with database at {cfg.Database.Path}" and "Server ready (full serve loop not yet implemented — Phase 2+)". This proves config loading and DB init work from the CLI. The actual HTTP/gRPC listeners come in later phases.

Add imports:
- `"github.com/claudeplane/claude-plane/internal/shared/tlsutil"`
- `"github.com/claudeplane/claude-plane/internal/server/store"`
- `"github.com/claudeplane/claude-plane/internal/server/config"`

Remove the blank proto import if it causes unused import issues (it was only there to prove compilation). If it still compiles with it, keep it.

Use `log/slog` for informational output (slog.Info) rather than fmt.Println.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane && go build -o /tmp/claude-plane-server ./cmd/server && /tmp/claude-plane-server ca init --out-dir /tmp/test-ca && ls /tmp/test-ca/ca.pem /tmp/test-ca/ca-key.pem && /tmp/claude-plane-server ca issue-server --ca-dir /tmp/test-ca --out-dir /tmp/test-server-cert && ls /tmp/test-server-cert/server.pem && /tmp/claude-plane-server ca issue-agent --ca-dir /tmp/test-ca --out-dir /tmp/test-agent-cert --machine-id test-nuc && ls /tmp/test-agent-cert/agent.pem && /tmp/claude-plane-server seed-admin --db /tmp/test-phase1.db --email admin@test.com --password testpass123 --name TestAdmin && rm -rf /tmp/test-ca /tmp/test-server-cert /tmp/test-agent-cert /tmp/test-phase1.db /tmp/claude-plane-server && echo "ALL SERVER COMMANDS VERIFIED"</automated>
  </verify>
  <done>All server CLI commands call their library implementations: ca init/issue-server/issue-agent generate real certificates, seed-admin creates a real admin user in SQLite, serve loads config and initializes the database. No "not implemented" stubs remain on the server side.</done>
</task>

<task type="auto">
  <name>Task 2: Wire agent run command to load config from TOML</name>
  <files>cmd/agent/main.go</files>
  <action>
Replace the "not implemented" stub in cmd/agent/main.go's newRunCmd with config loading.

1. **newRunCmd**: Add `--config` flag (string, required, default "agent.toml"). In RunE, call `config.LoadAgentConfig(configPath)`. On success, print (via slog.Info) "Agent config loaded" with fields: machine_id, server_address, max_sessions. Then print "Agent ready (gRPC connection not yet implemented — Phase 2)". The actual gRPC dial and PTY management come in Phase 2.

Add import:
- `"github.com/claudeplane/claude-plane/internal/agent/config"`

Remove the blank proto import if it causes unused import issues. If it still compiles with it, keep it.

2. After both files are updated, run `go vet ./...` and `go build ./cmd/...` to confirm everything compiles cleanly.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane && go build -o /dev/null ./cmd/agent && go vet ./... && go test ./... -count=1 -short && echo "AGENT WIRING VERIFIED"</automated>
  </verify>
  <done>Agent run command loads and validates TOML config. Invalid or missing config produces clear error messages. All existing tests still pass. No "not implemented" stubs remain except the agent's actual gRPC connection (Phase 2 scope).</done>
</task>

</tasks>

<verification>
- `go build -o /dev/null ./cmd/server` succeeds
- `go build -o /dev/null ./cmd/agent` succeeds
- `go vet ./...` passes
- `go test ./... -count=1` all tests pass (no regressions)
- `claude-plane-server ca init --out-dir /tmp/test-ca` generates ca.pem and ca-key.pem
- `claude-plane-server ca issue-server --ca-dir /tmp/test-ca --out-dir /tmp/test-sc` generates server.pem
- `claude-plane-server ca issue-agent --ca-dir /tmp/test-ca --out-dir /tmp/test-ac --machine-id nuc-01` generates agent.pem
- `claude-plane-server seed-admin --db /tmp/test.db --email admin@test.com --password secret` creates admin user
- `claude-plane-server serve --config server.toml` loads config and initializes DB (with a valid TOML file)
- `claude-plane-agent run --config agent.toml` loads config (with a valid TOML file)
</verification>

<success_criteria>
All Phase 1 verification gaps are closed. The four failed success criteria (SC2-SC5) now pass: CA commands generate real certs, serve command loads config and initializes SQLite, both binaries parse TOML config, and seed-admin creates an admin account. Zero "not implemented" stubs remain on the server side.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
