---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/server/store/db.go
  - internal/server/store/db_test.go
  - internal/server/store/migrations.go
  - internal/server/store/users.go
  - internal/server/store/users_test.go
autonomous: true
requirements:
  - INFR-03
  - AUTH-04

must_haves:
  truths:
    - "Server initializes SQLite database with WAL mode enabled"
    - "All schema tables are created on first run (users, machines, sessions, jobs, steps, step_dependencies, runs, run_steps, credentials, audit_log)"
    - "Single-writer pattern enforced: write pool has MaxOpenConns=1"
    - "Foreign keys are enabled"
    - "Admin account can be seeded with email and password"
    - "Admin password is hashed with Argon2id"
    - "Duplicate admin seeding fails gracefully (no crash, clear error)"
  artifacts:
    - path: "internal/server/store/db.go"
      provides: "Database initialization with dual connection pools"
      exports: ["Store", "NewStore", "Close"]
    - path: "internal/server/store/migrations.go"
      provides: "Schema creation SQL"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "internal/server/store/users.go"
      provides: "User CRUD and admin seeding"
      exports: ["SeedAdmin", "GetUserByEmail"]
    - path: "internal/server/store/db_test.go"
      provides: "Tests for DB initialization and WAL mode"
    - path: "internal/server/store/users_test.go"
      provides: "Tests for admin seeding"
  key_links:
    - from: "internal/server/store/db.go"
      to: "modernc.org/sqlite"
      via: "database/sql driver registration"
      pattern: "sql\\.Open.*sqlite"
    - from: "internal/server/store/users.go"
      to: "golang.org/x/crypto/argon2"
      via: "Argon2id password hashing"
      pattern: "argon2\\.IDKey"
---

<objective>
Implement the SQLite store layer with WAL mode, all schema tables, and admin account seeding with Argon2id password hashing.

Purpose: Provides persistent storage for the entire application and enables the admin seeding requirement (AUTH-04).
Output: Tested database layer with schema migration and user management.
</objective>

<execution_context>
@/home/joseibanez/.claude/get-shit-done/workflows/execute-plan.md
@/home/joseibanez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@docs/internal/product/backend_v1.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SQLite store with WAL mode and full schema</name>
  <files>internal/server/store/db.go, internal/server/store/db_test.go, internal/server/store/migrations.go</files>
  <behavior>
    - NewStore(dbPath) creates two sql.DB pools: writer (MaxOpenConns=1) and reader (MaxOpenConns=4)
    - DSN includes: _journal_mode=WAL, _busy_timeout=5000, _synchronous=NORMAL, _foreign_keys=ON
    - Reader DSN adds mode=ro
    - RunMigrations creates all tables: users, machines, sessions, jobs, steps, step_dependencies, runs, run_steps, credentials, audit_log (with indexes)
    - PRAGMA journal_mode returns "wal" after init
    - PRAGMA foreign_keys returns 1
    - Close() closes both pools
    - Tables use CREATE TABLE IF NOT EXISTS (idempotent)
  </behavior>
  <action>
1. Create `internal/server/store/db.go`:
   - `Store` struct with `writer *sql.DB` and `reader *sql.DB` fields
   - `NewStore(dbPath string) (*Store, error)`:
     - Writer DSN: `file:{dbPath}?_journal_mode=WAL&_busy_timeout=5000&_synchronous=NORMAL&_foreign_keys=ON`
     - Reader DSN: same but with `&mode=ro` appended
     - Import `modernc.org/sqlite` driver as `_ "modernc.org/sqlite"`
     - Set `writer.SetMaxOpenConns(1)`, `reader.SetMaxOpenConns(4)`
     - Call `RunMigrations` on the writer
   - `Close() error` — closes both pools
   - `Writer() *sql.DB` and `Reader() *sql.DB` accessors

2. Create `internal/server/store/migrations.go`:
   - `RunMigrations(db *sql.DB) error`
   - Execute the full schema SQL from the research doc: users, machines, sessions, jobs, steps, step_dependencies, runs, run_steps, credentials, audit_log tables plus all indexes
   - Use a single `db.Exec()` call with all CREATE TABLE IF NOT EXISTS statements
   - Use BEGIN IMMEDIATE for the transaction

3. Write tests first (TDD), use t.TempDir() for DB files:
   - TestNewStore: creates store, verifies WAL mode via PRAGMA, verifies foreign_keys ON
   - TestRunMigrations: runs migrations twice (idempotent), verifies all tables exist
   - TestSingleWriter: verify writer has MaxOpenConns=1
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane && go test ./internal/server/store/... -run TestNewStore -v -count=1 && go test ./internal/server/store/... -run TestRunMigrations -v -count=1</automated>
  </verify>
  <done>SQLite store initializes with WAL mode, all schema tables are created, single-writer pattern enforced, foreign keys enabled. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin seeding with Argon2id password hashing</name>
  <files>internal/server/store/users.go, internal/server/store/users_test.go</files>
  <behavior>
    - SeedAdmin(email, password, displayName) creates a user with role="admin"
    - Password is hashed with Argon2id (time=1, memory=64MB, threads=4, keyLen=32)
    - Hash format: $argon2id$v=19$m=65536,t=1,p=4$<salt>$<hash>
    - user_id is a UUID (generated via crypto/rand)
    - Duplicate email returns specific error (not a crash/panic)
    - GetUserByEmail returns user record or nil
    - VerifyPassword checks plaintext against stored Argon2id hash
  </behavior>
  <action>
1. Create `internal/server/store/users.go`:
   - `HashPassword(password string) (string, error)` — Argon2id with OWASP params: time=1, memory=64*1024, threads=4, keyLen=32. Format: `$argon2id$v=19$m=65536,t=1,p=4$<base64-salt>$<base64-hash>`
   - `VerifyPassword(password, hash string) (bool, error)` — parse the hash string, extract params/salt, recompute, compare with `subtle.ConstantTimeCompare`
   - `(s *Store) SeedAdmin(email, password, displayName string) error` — generates UUID for user_id, hashes password, inserts into users table with role="admin", uses BEGIN IMMEDIATE transaction on writer. On duplicate email (UNIQUE constraint violation), return a descriptive error.
   - `(s *Store) GetUserByEmail(email string) (*User, error)` — reads from reader pool, returns nil if not found
   - `User` struct: UserID, Email, DisplayName, PasswordHash, Role, CreatedAt, UpdatedAt

2. Write tests first (TDD):
   - TestHashAndVerify: hash a password, verify it matches, verify wrong password fails
   - TestSeedAdmin: seed admin, verify user exists via GetUserByEmail, verify role is "admin"
   - TestSeedAdminDuplicate: seed twice with same email, second call returns error (not panic)
   - TestGetUserByEmailNotFound: returns nil for non-existent email
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/claude-plane && go test ./internal/server/store/... -run "TestHash|TestSeed|TestGetUser" -v -count=1</automated>
  </verify>
  <done>Admin seeding creates user with Argon2id hashed password. Duplicate seeding fails gracefully. Password verification works. All tests pass.</done>
</task>

</tasks>

<verification>
- `go test ./internal/server/store/... -v -count=1` — all store tests pass
- `go vet ./...` passes
- Full suite: `go test ./... -count=1` passes (includes Plans 01 and 02 if complete)
</verification>

<success_criteria>
SQLite initializes with WAL mode and all schema tables. Admin can be seeded with Argon2id-hashed password. Duplicate seeding handled gracefully. All tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
