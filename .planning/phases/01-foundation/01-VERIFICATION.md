---
phase: 01-foundation
verified: 2026-03-12T10:00:00Z
status: passed
score: 5/5 success criteria verified
re_verification:
  previous_status: gaps_found
  previous_score: 2/5
  gaps_closed:
    - "CA CLI commands now call tlsutil.GenerateCA, IssueServerCert, IssueAgentCert"
    - "serve command now calls config.LoadServerConfig and store.NewStore"
    - "seed-admin command now calls store.NewStore and s.SeedAdmin"
    - "agent run command now calls config.LoadAgentConfig"
  gaps_remaining: []
  regressions: []
---

# Phase 1: Foundation Verification Report

**Phase Goal:** Both binaries compile and share a protobuf contract; CA tooling can generate certs for mTLS; SQLite schema is initialized with correct concurrency settings; both binaries parse TOML config files
**Verified:** 2026-03-12T10:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Running `go build` produces a server binary and an agent binary with no external runtime dependencies | VERIFIED | `go build -o /dev/null ./cmd/server` and `./cmd/agent` both succeed; modernc.org/sqlite is pure-Go |
| 2 | A CLI command generates a root CA, server certificate, and agent certificate that can be used for mTLS | VERIFIED | `newCAInitCmd` calls `tlsutil.GenerateCA`, `newCAIssueServerCmd` calls `tlsutil.IssueServerCert`, `newCAIssueAgentCmd` calls `tlsutil.IssueAgentCert`; all three tlsutil functions are tested including mTLS handshake |
| 3 | Server starts, initializes a SQLite database with WAL mode, and creates all schema tables | VERIFIED | `newServeCmd` calls `config.LoadServerConfig` then `store.NewStore`; `store.NewStore` sets WAL mode and calls `RunMigrations`; all store tests pass |
| 4 | Both binaries read configuration from a TOML file and fail with clear errors on missing required fields | VERIFIED | `newServeCmd` calls `config.LoadServerConfig(configPath)`; `newRunCmd` calls `config.LoadAgentConfig(configPath)`; both return wrapped errors on failure |
| 5 | An admin account can be seeded via server CLI command on first run | VERIFIED | `newSeedAdminCmd` calls `store.NewStore` then `s.SeedAdmin(email, password, name)`; validates `--email` and `--password` required; Argon2id hashing confirmed in store |

**Score:** 5/5 success criteria verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `go.mod` | Go module definition | VERIFIED | module github.com/kodrunhq/claude-plane; all Phase 1 deps present |
| `proto/claudeplane/v1/agent.proto` | gRPC service and message definitions | VERIFIED | AgentService with Register + CommandStream; 18 message types |
| `internal/shared/proto/claudeplane/v1/agent.pb.go` | Generated protobuf Go code | VERIFIED | Generated by buf; blank-imported in both binaries |
| `internal/shared/proto/claudeplane/v1/agent_grpc.pb.go` | Generated gRPC Go code | VERIFIED | Generated by buf; blank-imported in both binaries |
| `cmd/server/main.go` | Server binary entrypoint with wired subcommands | VERIFIED | All four subcommands (serve, ca init, ca issue-server, ca issue-agent, seed-admin) call their respective libraries; no stubs |
| `cmd/agent/main.go` | Agent binary entrypoint with wired run command | VERIFIED | `run` command calls `config.LoadAgentConfig`; gRPC connection deferred to Phase 2 as expected |
| `internal/shared/tlsutil/ca.go` | CA init and cert issuance | VERIFIED | GenerateCA, IssueServerCert, IssueAgentCert implemented |
| `internal/shared/tlsutil/loader.go` | TLS config builders | VERIFIED | ServerTLSConfig, AgentTLSConfig; ClientAuth=RequireAndVerifyClientCert |
| `internal/server/config/config.go` | Server TOML config | VERIFIED | LoadServerConfig; Validate with field-level errors |
| `internal/agent/config/config.go` | Agent TOML config | VERIFIED | LoadAgentConfig; defaults; Validate |
| `internal/server/store/db.go` | Database init with dual pools | VERIFIED | NewStore; writer MaxOpenConns=1; WAL mode; foreign_keys=ON |
| `internal/server/store/migrations.go` | Schema creation SQL | VERIFIED | RunMigrations; 10 tables; CREATE TABLE IF NOT EXISTS |
| `internal/server/store/users.go` | User CRUD and admin seeding | VERIFIED | SeedAdmin; Argon2id hashing |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `cmd/server/main.go` | `internal/shared/proto/claudeplane/v1` | blank import | WIRED | `_ "github.com/kodrunhq/claude-plane/internal/shared/proto/claudeplane/v1"` present |
| `cmd/agent/main.go` | `internal/shared/proto/claudeplane/v1` | blank import | WIRED | `_ "github.com/kodrunhq/claude-plane/internal/shared/proto/claudeplane/v1"` present |
| `cmd/server/main.go` | `internal/shared/tlsutil` | CA cobra commands call tlsutil | WIRED | `tlsutil.GenerateCA` (line 86), `tlsutil.IssueServerCert` (line 107), `tlsutil.IssueAgentCert` (line 134) |
| `cmd/server/main.go` | `internal/server/store` | serve and seed-admin call store | WIRED | `store.NewStore` called in both `newServeCmd` (line 51) and `newSeedAdminCmd` (line 165); `s.SeedAdmin` called at line 171 |
| `cmd/server/main.go` | `internal/server/config` | serve command loads config | WIRED | `config.LoadServerConfig(configPath)` called at line 46 |
| `cmd/agent/main.go` | `internal/agent/config` | run command loads config | WIRED | `config.LoadAgentConfig(configPath)` called at line 42 |
| `internal/shared/tlsutil/ca.go` | `crypto/x509` | x509.CreateCertificate for CA and leaf certs | WIRED | x509.CreateCertificate present on multiple lines |
| `internal/server/config/config.go` | `github.com/BurntSushi/toml` | toml.DecodeFile for config loading | WIRED | toml.DecodeFile confirmed |
| `internal/server/store/db.go` | `modernc.org/sqlite` | database/sql driver registration | WIRED | `_ "modernc.org/sqlite"` blank import present |
| `internal/server/store/users.go` | `golang.org/x/crypto/argon2` | Argon2id password hashing | WIRED | argon2.IDKey confirmed |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| INFR-01 | 01-01-PLAN.md | Server is a single Go binary with embedded frontend assets | SATISFIED (phase scope) | cmd/server/main.go compiles to a single binary; frontend embed is Phase 5 scope |
| INFR-02 | 01-01-PLAN.md | Agent is a single Go binary with no external dependencies | SATISFIED | cmd/agent/main.go compiles; modernc.org/sqlite is pure-Go (no CGo) |
| INFR-03 | 01-03-PLAN.md | Server uses SQLite with WAL mode for all persistent storage | SATISFIED | store.NewStore (WAL mode + migrations) called from newServeCmd; all store tests pass |
| INFR-04 | 01-02-PLAN.md | Server and agent support TOML configuration files | SATISFIED | LoadServerConfig wired in newServeCmd; LoadAgentConfig wired in newRunCmd; config tests pass |
| AGNT-01 | 01-02-PLAN.md | Server provides CA tooling to generate root CA, server certs, and agent certs | SATISFIED | tlsutil.GenerateCA, IssueServerCert, IssueAgentCert all wired into cobra CA commands |
| AUTH-04 | 01-03-PLAN.md | Admin account can be seeded via server CLI on first run | SATISFIED | store.SeedAdmin wired into newSeedAdminCmd; email/password validation present |

No orphaned requirements. All six Phase 1 IDs are covered and satisfied.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `cmd/server/main.go:58` | `slog.Info("Server ready (full serve loop not yet implemented — Phase 2+)")` | Info | Informational log after real initialization; serve loop is Phase 2 scope — not a stub |
| `cmd/agent/main.go:52` | `slog.Info("Agent ready (gRPC connection not yet implemented — Phase 2)")` | Info | Informational log after config load; gRPC connection is Phase 2 scope — not a stub |

No blockers. No warnings. The two info-level log messages are expected scope delimiters, not placeholder implementations — the commands do real work before reaching them.

---

### Human Verification Required

None. All checks are automated (build, test, code inspection).

---

### Re-Verification Summary

All four gaps from the initial verification have been closed:

1. **CA CLI wired** — `newCAInitCmd`, `newCAIssueServerCmd`, `newCAIssueAgentCmd` now call `tlsutil.GenerateCA`, `tlsutil.IssueServerCert`, and `tlsutil.IssueAgentCert` respectively. The `--machine-id` required flag validation is also present in `issue-agent`.

2. **Serve command wired** — `newServeCmd` now calls `config.LoadServerConfig` and `store.NewStore`, initializing the database with WAL mode and running all migrations. The serve loop itself is intentionally deferred to Phase 2.

3. **Config loading wired** — `newServeCmd` imports and calls `config.LoadServerConfig`; `newRunCmd` imports and calls `config.LoadAgentConfig`. Both return wrapped errors on failure, satisfying the "fail with clear errors" criterion.

4. **Seed-admin wired** — `newSeedAdminCmd` validates required `--email` and `--password` flags, opens the database via `store.NewStore`, and calls `s.SeedAdmin` with Argon2id hashing.

No regressions detected. Both binaries still compile. All 4 library test suites (tlsutil, store, server/config, agent/config) continue to pass.

---

_Verified: 2026-03-12T10:00:00Z_
_Verifier: Claude (gsd-verifier)_
