package version

// Version is the canonical version string for the claude-plane server.
// It is the single source of truth shared across the server binary and
// the gRPC service handlers.
const Version = "0.1.0"
